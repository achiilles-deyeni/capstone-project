# Backend Python dependencies for careerpath-ai
# Run `pip install -r requirements.txt` in your backend venv to install.

# Core web framework
fastapi
pydantic

# Production ASGI server (use with gunicorn in production)
uvicorn[standard]
gunicorn

# Google GenAI (Gemini) client
google-genai

# Environment variable loader
python-dotenv

# Database / ORM
SQLAlchemy

# File upload support
python-multipart

# Optional: Clerk backend SDK (comment out if not using)
clerk-backend-api

# Optional: markdown support
markdown
